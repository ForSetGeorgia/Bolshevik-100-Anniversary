<%
  page_title @story.title
  meta_title strip_tags(@story.title_share)
  meta_description strip_tags(@story.description_share)
  meta_image @story.image_share.url
%>
<%= render partial: 'shared/story_show', locals: {show_pubic_links: true} %>
